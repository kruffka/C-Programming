# 💻 Programming
  
1. Установите CMake: 
```bash
sudo apt install build-essential cmake -y
```
  
1. Задайте имя проекта (project) и минимальную версию (cmake_minimum_required).
Подключите поддиректории `background` и `pipes` через add_subdirectory.

**Важно:** В самих папках `background` и `pipes` уже есть свои CMakeLists.txt и 
их нужно дополнить парой строк для сборки (см. комментарии внутри них).
  
3. Решите проблему "Header not found": добавьте пути к заголовочным файлам через include_directories в этом файле.
Создайте исполняемый файл из src/main.c.
Привяжите (слинкуйте) библиотеку `background` к вашему исполняемому файлу.

4. Запустить результат сборки (исполняемый файл)