# Задание: задать имя проекта, версию cmake. добавить subdirectory для static и dynamic, 
# в которых тоже есть свои CMakeLists.txt (внутри каждого тоже нужно добавить код)
# Включить директорию include/
# Слинковать исполняемый файл из src/main.c с полученными библиотеками в поддиректориях
