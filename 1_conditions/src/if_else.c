#include <stdio.h>

int main() {
    int doorCode = 555; // Поменяй меня

    if (doorCode == 555) {
        printf("Correct code\n");
    } else {
        printf("Wrong code\n");
    }
    return 0;
}